Given /^a (product|book) named "([^"]*)" that costs \$(\d+\.?\d+)$/ do |type, name, price|
  # This step is for completeness; datastore is specified in lib/item.rb
end

Given /^a product named "([^"]*)" that is free$/ do |name|
  # This step is for completeness; datastore is specified in lib/item.rb
end


